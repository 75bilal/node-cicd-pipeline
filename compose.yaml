services:
  db:
    image: mongo
    ports:
      - 27017:27017
    volumes:
      - mongodb_data:/data/db
  node-server:
    build:
      context: .
      dockerfile: ./Dockerfile
    env_file:
      - .env
    ports:
      - 5000:5000

volumes:
  mongodb_data:
    driver: local